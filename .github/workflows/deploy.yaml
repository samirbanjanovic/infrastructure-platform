name: Deploy Infrastructure

# trigger workflow manually from the Actions tab
on:
    workflow_dispatch:
        inputs:
            logLevel:
                description: 'Log level'
                required: true
                default: 'warning'
                type: choice
                options:
                - info
                - warning
                - debug

jobs:
    deploy-dev:
        name: Development
        uses: ./.github/workflows/control-plane-template
        with:
            Environment: development
        secrets: inherit